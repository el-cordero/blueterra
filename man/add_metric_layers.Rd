% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom-metrics.R
\name{add_metric_layers}
\alias{add_metric_layers}
\title{Add metric layers to an existing raster stack}
\usage{
add_metric_layers(
  metrics,
  ...,
  names = NULL,
  overwrite = FALSE,
  check_geometry = TRUE
)
}
\arguments{
\item{metrics}{A \code{terra::SpatRaster} metric stack or raster input accepted by
\code{\link[=as_bathy]{as_bathy()}}.}

\item{...}{One or more \code{terra::SpatRaster} objects or local raster paths to
add.}

\item{names}{Optional names for the added layers.}

\item{overwrite}{Logical. Allow added layers to replace layers with matching
names in \code{metrics}.}

\item{check_geometry}{Logical. Require matching CRS, extent, resolution,
dimensions, and origin.}
}
\value{
A combined \code{terra::SpatRaster}.
}
\description{
Combines precomputed metric rasters with an existing metric stack after
checking grid geometry.
}
\details{
Custom metrics must be on the same raster grid as the stack they extend.
Geometry checks are enabled by default because summaries, PCA tables, and
process-group assignments assume cell-aligned layers.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
metrics <- derive_terrain(bathy, metrics = c("slope", "tri"))
index <- derive_custom_metric(metrics, "slope_tri_index", expression = quote(slope_deg * tri))
add_metric_layers(metrics, index)

}
\seealso{
\code{\link[=derive_custom_metric]{derive_custom_metric()}}, \code{\link[=create_metric_catalog]{create_metric_catalog()}}
}
