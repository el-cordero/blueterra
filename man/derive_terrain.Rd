% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{derive_terrain}
\alias{derive_terrain}
\title{Derive bathymetric terrain metrics}
\usage{
derive_terrain(
  x,
  metrics = "default",
  scales = NULL,
  units = c("degrees", "radians"),
  neighbors = 8,
  positive_depth = NULL,
  filename = "",
  overwrite = FALSE,
  progress = TRUE
)
}
\arguments{
\item{x}{A raster-like object accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{metrics}{Character vector of metrics, or \code{"default"}.}

\item{scales}{Optional BPI/TPI window sizes used when multiscale BPI is
requested.}

\item{units}{Slope and aspect units, \code{"degrees"} or \code{"radians"}.}

\item{neighbors}{Neighborhood passed to \code{terra::terrain()}.}

\item{positive_depth}{Optional logical documenting the input sign convention.
Metric values are not sign-flipped unless a specific function says so.}

\item{filename}{Optional output raster path.}

\item{overwrite}{Logical. Allow overwriting \code{filename}.}

\item{progress}{Logical. Reserved for long-running workflows.}
}
\value{
A \code{terra::SpatRaster} containing named metric layers.
}
\description{
Computes one or more terrain metrics from a user-supplied bathymetric or
elevation raster.
}
\details{
Terrain metrics are scale-sensitive. Slope, curvature, rugosity, TPI, BPI,
roughness, and surface-area style metrics depend on grid resolution,
smoothing, and focal-window size. Use projected coordinate systems when
distances or slopes are interpreted in real linear units.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
terrain <- derive_terrain(bathy, metrics = c("slope", "aspect", "bpi"))
names(terrain)

}
\seealso{
\code{\link[=derive_metric_stack]{derive_metric_stack()}}, \code{\link[=derive_bpi]{derive_bpi()}}, \code{\link[=metric_catalog]{metric_catalog()}}
}
