% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{derive_bpi}
\alias{derive_bpi}
\alias{derive_multiscale_bpi}
\title{Derive bathymetric position index}
\usage{
derive_bpi(
  x,
  inner_radius = NULL,
  outer_radius = NULL,
  window = NULL,
  scale = c("fine", "broad", "custom"),
  normalize = FALSE,
  filename = "",
  overwrite = FALSE,
  ...
)

derive_multiscale_bpi(
  x,
  windows = c(3, 11),
  normalize = FALSE,
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{x}{A raster-like object accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{inner_radius}{Optional inner radius for an annulus window in map units.}

\item{outer_radius}{Optional outer radius for an annulus window in map units.}

\item{window}{Optional odd integer square window size in cells.}

\item{scale}{Preset scale when \code{window} and \code{outer_radius} are not supplied.}

\item{normalize}{Logical. If \code{TRUE}, divide BPI by local focal standard
deviation. Zero-variance or unavailable focal neighborhoods return \code{NA}.}

\item{filename}{Optional output raster path.}

\item{overwrite}{Logical. Allow overwriting \code{filename}.}

\item{...}{Reserved for future extensions.}

\item{windows}{Integer vector of odd square window sizes.}
}
\value{
A single-layer \code{terra::SpatRaster}.
}
\description{
Computes a local bathymetric position index from the difference between each
cell and its focal-neighborhood mean.
}
\details{
The calculation is \verb{cell value - focal mean}. Positive values therefore mean
higher-than-neighborhood values when the raster is elevation-like. For
positive-depth rasters, interpretation is reversed unless users convert the
sign convention first. Square windows are measured in cells and include the
focal cell. Annular windows are measured in map units, require a projected
CRS with linear units, and use separate x- and y-cell resolutions when cells
are not square. At raster edges and next to missing cells, BPI uses the
available cells in its partial focal support; a missing focal value remains
missing.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
derive_bpi(bathy, window = 5)

}
\seealso{
\code{\link[=derive_multiscale_bpi]{derive_multiscale_bpi()}}, \code{\link[=derive_tpi]{derive_tpi()}}
}
