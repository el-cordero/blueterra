% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling-helpers.R
\name{terrain_pca}
\alias{terrain_pca}
\title{Principal components analysis for terrain tables}
\usage{
terrain_pca(data, vars = NULL, center = TRUE, scale. = TRUE, ...)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Optional character vector of numeric variables.}

\item{center}{Logical passed to \code{stats::prcomp()}.}

\item{scale.}{Logical passed to \code{stats::prcomp()}.}

\item{...}{Additional arguments passed to \code{stats::prcomp()}.}
}
\value{
A list with \code{scores}, \code{loadings}, \code{variance}, and \code{model}.
}
\description{
Runs PCA on numeric terrain variables and returns tidy scores, loadings,
variance explained, and the fitted model.
}
\details{
Rows with incomplete values in selected variables are omitted before PCA.
PCA is descriptive and should be interpreted with scale, CRS, and sampling
design in mind.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
terrain <- derive_terrain(bathy, metrics = c("slope", "bpi", "roughness"))
cells <- sample_terrain_cells(terrain, size = 30)
terrain_pca(cells)

}
\seealso{
\code{\link[=prepare_model_matrix]{prepare_model_matrix()}}, \code{\link[=terrain_correlation]{terrain_correlation()}}
}
