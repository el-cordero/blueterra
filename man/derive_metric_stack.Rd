% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{derive_metric_stack}
\alias{derive_metric_stack}
\title{Build a stack of terrain metrics}
\usage{
derive_metric_stack(
  x,
  metrics = "default",
  scales = NULL,
  units = c("degrees", "radians"),
  neighbors = 8,
  positive_depth = NULL,
  filename = "",
  overwrite = FALSE,
  progress = TRUE
)
}
\arguments{
\item{x}{A raster-like object accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{metrics}{Character vector of metrics, or \code{"default"}.}

\item{scales}{Optional BPI/TPI window sizes used when multiscale BPI is
requested.}

\item{units}{Slope and aspect units, \code{"degrees"} or \code{"radians"}.}

\item{neighbors}{Neighborhood passed to \code{terra::terrain()}.}

\item{positive_depth}{Optional logical documenting the input sign convention.
Metric values are not sign-flipped unless a specific function says so.}

\item{filename}{Optional output raster path.}

\item{overwrite}{Logical. Allow overwriting \code{filename}.}

\item{progress}{Logical. Reserved for long-running workflows.}
}
\value{
A \code{terra::SpatRaster}.
}
\description{
Computes selected metrics and returns them as a named raster stack.
}
\details{
\code{"default"} currently expands to bathymetry, slope, aspect, northness,
eastness, hillshade, roughness, TRI, TPI, fine BPI, broad BPI, curvature, and
surface-area ratio. All metrics are derived locally from the supplied raster.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
derive_metric_stack(bathy, metrics = "default")

}
\seealso{
\code{\link[=derive_terrain]{derive_terrain()}}
}
