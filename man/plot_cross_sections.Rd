% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transects.R
\name{plot_cross_sections}
\alias{plot_cross_sections}
\title{Plot sampled cross-sections}
\usage{
plot_cross_sections(
  samples,
  value_col = NULL,
  group_col = "transect_id",
  color_col = NULL,
  show_legend = TRUE,
  points = FALSE,
  mean_profile = FALSE,
  mean_profile_na_rm = TRUE,
  normalize_distance = FALSE,
  profile_direction = c("top_to_bottom", "bottom_to_top", "as_sampled", "max_to_min",
    "min_to_max", "high_to_low", "low_to_high"),
  positive_depth = NULL,
  depth_increases_down = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{samples}{Output from \code{\link[=sample_transects]{sample_transects()}}.}

\item{value_col}{Optional value column.}

\item{group_col}{Grouping column for transect lines.}

\item{color_col}{Optional column used to color profiles. Defaults to
\code{group_col}.}

\item{show_legend}{Logical. Show the line-color legend.}

\item{points}{Logical. Draw sample points over profile lines.}

\item{mean_profile}{Logical. Overlay an interpolated mean profile across
transects.}

\item{mean_profile_na_rm}{Logical. Remove missing interpolated profile values
when averaging the mean profile. The default, \code{TRUE}, lets the mean profile
use the full available distance range rather than stopping where the
shortest transect ends. Set to \code{FALSE} to require every profile to
contribute at each distance.}

\item{normalize_distance}{Logical. Plot distance as 0-1 normalized position
along each transect.}

\item{profile_direction}{Direction used to orient distance before plotting.
\code{"top_to_bottom"} (the default) orients bathymetric or elevation profiles
from the shallow or top endpoint toward the deeper or bottom endpoint.
With negative-elevation bathymetry this means higher numeric values to
lower numeric values. With positive-depth bathymetry, set
\code{positive_depth = TRUE} so the profile runs from lower depth values to
higher depth values. \code{"bottom_to_top"} reverses that convention.
\code{"max_to_min"} and \code{"min_to_max"} provide explicit numeric endpoint
controls for metrics, and \code{"as_sampled"} preserves the sampled line order.
Legacy values \code{"high_to_low"} and \code{"low_to_high"} are accepted as aliases
for \code{"top_to_bottom"} and \code{"bottom_to_top"}.}

\item{positive_depth}{Logical depth convention for \code{value_col}. This affects
top-to-bottom profile orientation for depth-like variables and y-axis
display for positive-depth values.}

\item{depth_increases_down}{Logical. If \code{TRUE}, positive-depth profiles are
plotted with a reversed y-axis so larger depths appear lower in the panel.}

\item{title, subtitle, caption}{Plot text.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots raster values against transect distance.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  bathy <- read_bathy(blueterra_example("bathy"))
  zones <- terra::vect(blueterra_example("zones"))
  transects <- make_transects(zones[1, ], spacing = 100, bathy = bathy)
  samples <- sample_transects(transects, bathy, n = 5)
  plot_cross_sections(samples)
}

}
\seealso{
\code{\link[=sample_transects]{sample_transects()}}, \code{\link[=plot_depth_profile]{plot_depth_profile()}}
}
