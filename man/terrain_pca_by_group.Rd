% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling-helpers.R
\name{terrain_pca_by_group}
\alias{terrain_pca_by_group}
\title{Run PCA overall and within groups}
\usage{
terrain_pca_by_group(
  data,
  group,
  vars = NULL,
  center = TRUE,
  scale. = TRUE,
  min_rows = 5,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{group}{Character name of the grouping column.}

\item{vars}{Optional numeric variables used in PCA.}

\item{center, scale.}{Logical values passed to \code{\link[=terrain_pca]{terrain_pca()}}.}

\item{min_rows}{Minimum rows required for a group-specific PCA.}

\item{...}{Additional arguments passed to \code{\link[=terrain_pca]{terrain_pca()}}.}
}
\value{
A named list with \code{overall} and \code{groups}. \code{groups} is a named list of
PCA objects.
}
\description{
Fits one terrain PCA across all rows and one PCA within each group level.
}
\details{
Group-specific PCA is useful for checking whether ordination structure is
dominated by one site or sampling frame. Groups with fewer than \code{min_rows}
rows are omitted with a warning.
}
\examples{
df <- data.frame(site = rep(c("a", "b"), each = 8), slope = rnorm(16),
                 tri = rnorm(16), bpi = rnorm(16))
terrain_pca_by_group(df, group = "site")

}
\seealso{
\code{\link[=terrain_pca]{terrain_pca()}}, \code{\link[=plot_process_pca]{plot_process_pca()}}
}
