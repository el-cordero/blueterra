% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transects.R
\name{estimate_surface_orientation}
\alias{estimate_surface_orientation}
\title{Estimate mean surface orientation from a bathymetric raster}
\usage{
estimate_surface_orientation(
  bathy,
  area = NULL,
  orientation_weight = c("slope", "none"),
  min_slope = 0,
  return = c("transect_angle", "bearing", "both")
)
}
\arguments{
\item{bathy}{A single-layer bathymetric or elevation raster, or a raster input
accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{area}{Optional polygon area used to crop and mask \code{bathy} before
estimating orientation.}

\item{orientation_weight}{Weighting method. \code{"slope"} weights aspect
components by slope magnitude; \code{"none"} averages finite aspect cells
equally.}

\item{min_slope}{Minimum slope in degrees retained when
\code{orientation_weight = "slope"}.}

\item{return}{Return type: \code{"transect_angle"} for the mathematical line angle
used by \code{\link[=make_transects]{make_transects()}}, \code{"bearing"} for the mean compass aspect, or
\code{"both"} for a tibble containing both values and the number of cells used.}
}
\value{
A numeric angle for \code{"transect_angle"} or \code{"bearing"}, or a tibble
with \code{bearing_deg}, \code{transect_angle_deg}, \code{orientation_weight},
\code{min_slope}, and \code{n_orientation_cells} when \code{return = "both"}.
}
\description{
Estimates the mean aspect direction of a raster surface and converts that
compass bearing to the line-angle convention used by \code{\link[=make_transects]{make_transects()}}.
}
\details{
Aspect is treated as a compass bearing where northness is \code{cos(aspect)} and
eastness is \code{sin(aspect)}. Mean circular components are converted to a
compass bearing with \code{atan2(eastness, northness)}. Transect lines are
undirected, so the line angle is normalized to \verb{[0, 180)}. A south-facing
mean aspect near 180 degrees therefore produces a transect angle near 90
degrees.
}
\examples{
bathy <- read_bathy(blueterra_example("hitw"))
zones <- terra::vect(blueterra_example("zones"))
hitw <- zones[zones$site_id == "hitw", ]
estimate_surface_orientation(bathy, hitw)

}
\seealso{
\code{\link[=make_transects]{make_transects()}}, \code{\link[=derive_aspect]{derive_aspect()}}, \code{\link[=derive_slope]{derive_slope()}}
}
