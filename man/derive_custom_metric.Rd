% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom-metrics.R
\name{derive_custom_metric}
\alias{derive_custom_metric}
\title{Derive a custom metric from raster layers}
\usage{
derive_custom_metric(
  metrics,
  name,
  expression = NULL,
  fun = NULL,
  ...,
  overwrite = FALSE
)
}
\arguments{
\item{metrics}{A \code{terra::SpatRaster} metric stack.}

\item{name}{Output layer name.}

\item{expression}{Optional quoted R expression evaluated with raster layers
available by name.}

\item{fun}{Optional function called as \code{fun(metrics, ...)}. The function must
return a single-layer \code{terra::SpatRaster}.}

\item{...}{Additional arguments passed to \code{fun}.}

\item{overwrite}{Logical. Allow \code{name} to match an existing layer in
\code{metrics}.}
}
\value{
A single-layer \code{terra::SpatRaster} named \code{name}.
}
\description{
Creates a single custom metric layer from an expression or a user-supplied R
function.
}
\details{
Expressions must be quoted, for example
\code{quote(slope_deg * rugosity_vrm_3x3)}. Character strings are not evaluated.
This keeps the workflow explicit and avoids treating text as code.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
metrics <- derive_terrain(bathy, metrics = c("slope", "tri", "bpi"))
derive_custom_metric(metrics, "slope_tri_index", expression = quote(slope_deg * tri))

relief <- derive_custom_metric(metrics, "relief_index", fun = function(r) {
  out <- r[["tri"]] + abs(r[["bpi_3x3"]])
  names(out) <- "relief_index"
  out
})
relief

}
\seealso{
\code{\link[=add_metric_layers]{add_metric_layers()}}
}
