% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_bathy}
\alias{prepare_bathy}
\title{Prepare a bathymetric or elevation raster}
\usage{
prepare_bathy(
  x,
  crs = NULL,
  resolution = NULL,
  extent = NULL,
  mask = NULL,
  depth_range = NULL,
  positive_depth = NULL,
  method = "bilinear",
  smooth = FALSE,
  smooth_window = 3,
  filename = "",
  overwrite = FALSE
)
}
\arguments{
\item{x}{A raster-like object accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{crs}{Optional target CRS passed to \code{\link[=project_bathy]{project_bathy()}}.}

\item{resolution}{Optional target cell size. A single value is used for both
axes; two values are used as x and y resolution.}

\item{extent}{Optional crop extent: \code{SpatExtent}, numeric
\verb{xmin, xmax, ymin, ymax}, raster, or vector object.}

\item{mask}{Optional polygon/vector mask.}

\item{depth_range}{Optional numeric length-two range of retained depths or
elevations.}

\item{positive_depth}{Optional logical. If \code{TRUE}, convert output to positive
depth. If \code{FALSE}, convert output to negative depth/elevation. If \code{NULL},
preserve sign convention.}

\item{method}{Resampling and projection method passed to \code{terra}.}

\item{smooth}{Logical. If \code{TRUE}, apply \code{\link[=smooth_bathy]{smooth_bathy()}}.}

\item{smooth_window}{Odd integer focal-window size for smoothing.}

\item{filename}{Optional output raster path.}

\item{overwrite}{Logical. Allow overwriting \code{filename}.}
}
\value{
A \code{terra::SpatRaster}.
}
\description{
Applies common preprocessing steps for terrain analysis: optional projection,
resampling, cropping, masking, depth filtering, sign conversion, smoothing,
and optional file-backed output.
}
\details{
\code{prepare_bathy()} never automatically reprojects or flips depth signs. Those
operations occur only when \code{crs} or \code{positive_depth} are supplied. Distance-
based geomorphometry should generally be performed in a projected CRS with
linear units.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
prepared <- prepare_bathy(bathy, depth_range = c(-90, -20), smooth = TRUE)
prepared

}
\seealso{
\code{\link[=crop_bathy]{crop_bathy()}}, \code{\link[=mask_bathy]{mask_bathy()}}, \code{\link[=depth_filter]{depth_filter()}}
}
