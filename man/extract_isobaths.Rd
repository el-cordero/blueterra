% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isobath-corridors.R
\name{extract_isobaths}
\alias{extract_isobaths}
\title{Extract isobaths from a raster}
\usage{
extract_isobaths(
  x,
  depths,
  positive_depth = NULL,
  as = c("sf", "SpatVector"),
  ...
)
}
\arguments{
\item{x}{A raster-like object accepted by \code{\link[=as_bathy]{as_bathy()}}.}

\item{depths}{Numeric contour levels.}

\item{positive_depth}{Optional logical. If \code{TRUE}, \code{depths} are converted to
positive values. If \code{FALSE}, they are converted to negative values. If
\code{NULL}, \code{depths} are used exactly as supplied.}

\item{as}{Output type: \code{"sf"} or \code{"SpatVector"}.}

\item{...}{Additional arguments reserved for future extensions.}
}
\value{
Isobath line features as \code{sf} or \code{terra::SpatVector}.
}
\description{
Converts raster contour levels to line features.
}
\details{
Depth convention is explicit. For rasters stored as negative elevation,
either pass negative \code{depths} or set \code{positive_depth = FALSE} when passing
positive depth labels.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
extract_isobaths(bathy, depths = c(-40, -60))

}
\seealso{
\code{\link[=make_isobath_corridors]{make_isobath_corridors()}}
}
