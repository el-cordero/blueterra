% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process-groups.R
\name{assign_process_groups}
\alias{assign_process_groups}
\title{Assign metrics to process groups}
\usage{
assign_process_groups(x, catalog = metric_catalog(), unmatched = "unassigned")
}
\arguments{
\item{x}{A \code{terra::SpatRaster}, character vector, or data frame with metric
columns.}

\item{catalog}{Optional catalog table. Defaults to \code{\link[=metric_catalog]{metric_catalog()}}.}

\item{unmatched}{Character value assigned to unmatched metrics.}
}
\value{
A tibble with one row per supplied metric.
}
\description{
Matches raster layer names or character metric names to the metric catalog.
}
\details{
Matching uses standardized lower-case metric names. Unmatched metrics are
returned with \code{process_group = unmatched} so users can inspect custom layers.
}
\examples{
terrain <- derive_terrain(read_bathy(blueterra_example("bathy")))
assign_process_groups(terrain)

}
\seealso{
\code{\link[=metric_catalog]{metric_catalog()}}, \code{\link[=summarize_process_groups]{summarize_process_groups()}}
}
