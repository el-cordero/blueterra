% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling-helpers.R
\name{pca_axis_labels}
\alias{pca_axis_labels}
\title{Label PCA axes with variance and dominant loadings}
\usage{
pca_axis_labels(
  pca,
  components = c("PC1", "PC2"),
  top_n = 2,
  unique = TRUE,
  include_variance = TRUE
)
}
\arguments{
\item{pca}{Output from \code{\link[=terrain_pca]{terrain_pca()}}.}

\item{components}{Components to label.}

\item{top_n}{Number of high-loading variables included per component.}

\item{unique}{Logical. Avoid repeating the same dominant variable across
component labels when possible.}

\item{include_variance}{Logical. Include percent variance explained.}
}
\value{
A named character vector of axis labels.
}
\description{
Builds axis labels for \code{\link[=plot_process_pca]{plot_process_pca()}} from a \code{terrain_pca()} object.
}
\details{
Variables are ranked by absolute loading for each component. These labels are
descriptive aids for ordination plots; they do not replace inspection of the
full loading table.
}
\examples{
df <- data.frame(a = rnorm(10), b = rnorm(10), c = rnorm(10))
pca_axis_labels(terrain_pca(df))

}
\seealso{
\code{\link[=terrain_pca]{terrain_pca()}}, \code{\link[=plot_process_pca]{plot_process_pca()}}
}
