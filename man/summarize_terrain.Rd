% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{summarize_terrain}
\alias{summarize_terrain}
\alias{summarize_terrain_by_zone}
\title{Summarize terrain metrics by polygon zones}
\usage{
summarize_terrain(
  metrics,
  zones,
  fun = c("mean", "sd", "min", "max", "median"),
  na.rm = TRUE,
  exact = FALSE,
  ...
)

summarize_terrain_by_zone(
  metrics,
  zones,
  fun = c("mean", "sd", "min", "max", "median"),
  na.rm = TRUE,
  exact = FALSE,
  ...
)
}
\arguments{
\item{metrics}{A metric raster stack.}

\item{zones}{Polygon zones as \code{sf}, \code{terra::SpatVector}, or a local vector
path.}

\item{fun}{Summary functions. Supported values are \code{"mean"}, \code{"sd"},
\code{"min"}, \code{"max"}, \code{"median"}, \code{"sum"}, and \code{"count"}.}

\item{na.rm}{Logical. Remove missing values before summarizing.}

\item{exact}{Logical. If \code{TRUE}, use \code{exactextractr} for extraction. The
optional package must be installed.}

\item{...}{Additional arguments passed to extraction functions.}
}
\value{
A tibble with zone identifiers, zone attributes, and wide summary
columns named \code{metric_function}.
}
\description{
Computes summary statistics for raster metrics inside polygons, buffers, or
corridor features.
}
\details{
\code{summarize_terrain()} does not assume specific zones, depth ranges, or
ecological labels. For distance-sensitive summaries, use zones and rasters in
a projected CRS.
}
\examples{
bathy <- read_bathy(blueterra_example("bathy"))
terrain <- derive_terrain(bathy, metrics = c("slope", "bpi"))
zones <- terra::vect(blueterra_example("zones"))
summarize_terrain(terrain, zones)

}
\seealso{
\code{\link[=summarize_depth_bands]{summarize_depth_bands()}}, \code{\link[=extract_terrain_points]{extract_terrain_points()}}
}
